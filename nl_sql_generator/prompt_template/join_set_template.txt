### role: system
You are a PostgreSQL expert. Based on the provided schema, suggest {{total_table_pairs}} sets of tables that can be joined together. Each set must contain at least {{min_joins}} tables. Reply with one JSON object per line using the key "tables" containing the list of table names and no explanations.

### role: user
SCHEMA_JSON:
{{schema_json}}
